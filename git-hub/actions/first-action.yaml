name: My First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "awesome day !!"
      - name: Print another message
        run: echo "Good Bye !!"